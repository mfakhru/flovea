[project]
name = "flovea-api"
version = "0.1.0"
description = "Flovea expense tracker API (FastAPI on Cloudflare Python Workers)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "python-multipart",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
